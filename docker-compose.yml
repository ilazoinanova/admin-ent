version: '3.8'

services:
  frontend:
    image: node:22
    container_name: admin_ent_frontend
    working_dir: /app
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
